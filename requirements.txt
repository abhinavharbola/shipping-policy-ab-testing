# Sole dependency manifest for this project (no pyproject.toml).
# Core pipeline deps + the dashboard and dev extras, in one file since
# nothing here is installed or distributed as a package.

# core
pandas
numpy
scipy
statsmodels

# dashboard extra
streamlit
plotly

# dev extra
pytest
pyflakes



